# check success of step ${last_step}:
retcode=$$?
if [ $$retcode != 0 ]; then
  echo ${last_step} failed
  python provenance update -p '${pipeline.name}' --status 'failed in step ${last_step}'
  exit $$retcode
fi
